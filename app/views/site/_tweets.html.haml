-#-@tweets.each do |key, tweet|
-#  %div{:class => "footnote"}
-#    =highlight(tweet[0]['text'].to_s, key.to_s.humanize.downcase)

:javascript
  function DoTweets() {
     alert("tweets loaded!");
     $('#loading').hide("slow");
     var example = Popcorn.youtube(
     '#video',
     'http://www.youtube.com/watch?v=ojbEWJFF3Dw' );
     #{tweets_help}
     example.play();
    };